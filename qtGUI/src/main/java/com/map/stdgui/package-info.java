/**
 * Reusable JavaFX abstractions for the QT GUI module.
 * <p>
 * This package centralizes thread dispatch, modal interactions, file selection,
 * clipboard access, status feedback, and view/window composition so that
 * application code can avoid low-level JavaFX plumbing where a stable library
 * API is sufficient.
 */
package com.map.stdgui;
